test_announcement:
  name: Test Announcement
  description: Test the time announcement immediately with current time
  fields:
    entity_id:
      name: Entity
      description: Any entity of the Digital Pendulum to test (switch, button or sensor). Leave empty to test all clocks.
      required: false
      selector:
        entity:
          integration: digital_pendulum
          multiple: true
